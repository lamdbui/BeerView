<?xml version="1.0" encoding="utf-8"?>
<manifest package="app.com.lamdbui.android.beerview"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/BeerMapper.Theme">
        <activity
            android:name=".BeerViewActivity"
            android:label="@string/app_name"
            android:theme="@style/BeerMapper.Theme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key"/>

        <activity
            android:name=".BrewViewMapsActivity"
            android:label="@string/title_activity_brew_view_maps">
        </activity>
        <activity
            android:name=".BreweryActivity"
            android:label="@string/title_activity_brewery">
        </activity>

        <provider
            android:name=".data.BreweryProvider"
            android:authorities="@string/content_authority"
            android:exported="false"/>

        <activity
            android:name=".BeerDetailActivity2"
            android:label="@string/title_activity_beer_detail2"
            android:theme="@style/BeerMapper.Theme.NoActionBar">
        </activity>
        <activity
            android:name=".BreweryDetailActivity"
            android:label="@string/title_activity_brewery_detail"
            android:theme="@style/BeerMapper.Theme.NoActionBar">
        </activity>
        <activity
            android:name=".BeerNavigationActivity"
            android:label="@string/title_activity_beer_home"
            android:theme="@style/BeerMapper.Theme.NoActionBar">
        </activity>
    </application>

</manifest>